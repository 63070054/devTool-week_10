version: '3'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: kapraokai/my-backend_image:1.0
  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    image: kapraokai/my-frontend_image:1.0
